# ST-Link 调试器配置文件
# 使用 ST-Link 硬件进行调试

source [find interface/stlink.cfg]

# OpenOCD作为GDB和硬件调试器的桥梁
# OpenOCD会自动在其根目录的share/openocd/scripts/interface里面寻找对应的配置文件

# 选择接口为SWD (stlink.cfg 已默认选择，此处注释)
# transport select swd

# 选择目标芯片
source [find target/stm32f4x.cfg]
